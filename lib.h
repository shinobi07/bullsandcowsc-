#include <iostream>
using namespace std;

int pow(int number, int exponent);
int splitting_digits(int number, int position);
int len(int number);
bool legitimate_number(string input);
void print_array(string user_input[], int guesses);
